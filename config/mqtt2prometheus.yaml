mqtt:
  broker: ${MQTT_BROKER}
  client_id: mqtt2prometheus
  username: ${MQTT_USERNAME}
  password: ${MQTT_PASSWORD}
  qos: 1
  clean_session: false

server:
  listen: ":9000"

log:
  level: ${MQTT2PROMETHEUS_LOG_LEVEL}

sources: sources/*.yaml
