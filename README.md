# mqtt2prometheus

Subscribes to MQTT topics and exposes selected messages as Prometheus metrics.
The mapping from topic to metric lives entirely in config.

Documentation lands with the implementation.
